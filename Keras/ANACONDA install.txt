ANACONDA install
All Users check
PATH check

명령 프롬프트에서 python 입력
ctrl Z로 빠져나간 후, pip install tensorflow(설치)

python들어간 후, import tensorflow as tf
tf.__version__ or print(tf.__version__)

pip install keras(설치)
pip install sklearn(설치)
